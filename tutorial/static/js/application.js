// Generated by CoffeeScript 1.3.3
(function() {
  var bind_slide_keys;

  window.jQuery.fn.center = function() {
    this.css("position", "absolute");
    this.css("top", Math.max(0, (($(window).height() - this.outerHeight()) / 2) + $(window).scrollTop()) + "px");
    this.css("left", Math.max(0, (($(window).width() - this.outerWidth()) / 2) + $(window).scrollLeft()) + "px");
    return this;
  };

  this.cardSide = "question";

  this.oppositeSide = function(side) {
    if (side === "question") {
      return "answer";
    }
    if (side === "answer") {
      return "question";
    }
  };

  this.updateCard = function() {
    $("#card-" + this.cardSide).show();
    return $("#card-" + (oppositeSide(this.cardSide))).hide();
  };

  this.flipCard = function() {
    console.log("flipping");
    this.cardSide = oppositeSide(this.cardSide);
    return updateCard();
  };

  $(document).ready(function() {
    return updateCard();
  });

  bind_slide_keys = function() {
    return $('body').keydown = function(event) {
      if (event.keyCode === 37) {
        return window.location.href = prev_url;
      } else if (event.keyCode === 39) {
        return window.location.href = next_url;
      }
    };
  };

}).call(this);
